---
name: Bug report
about: Something is drawn wrong, behaves wrong or crashes
labels: bug
---

**What happened**

A short description, and what you expected instead.

**Steps to reproduce**

1.
2.
3.

If it happens in your own application, a small `App` that shows it helps most. If it happens in the
showcase, name the page and section.

**Screenshot**

A screenshot or a short recording, if the problem is visible. Drawing problems are often specific to
one terminal or font, so a picture saves a lot of guessing.

**Environment**

- quvyta-framework version (or showcase version, `cargo install --list | grep quvyta`):
- Terminal and its version (kitty, WezTerm, Alacritty, GNOME Terminal, ...):
- Font:
- Operating system:
- Glyph mode (Nerd Font, Unicode or ASCII; set with `QUVYTA_ICONS=nerd|unicode|ascii` to compare):
- Theme:
- Language:
- Terminal size (columns x rows), if the problem depends on it:
- Reduced motion on or off, if the problem is about animation:
