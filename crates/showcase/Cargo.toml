[package]
name = "showcase"
description = "Internal showcase of the Quvyta framework: live demos, code, guides and references"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
quvyta-framework = { path = "../quvyta-framework", features = ["pty"] }
toml = { version = "1.1.6", default-features = false, features = ["std", "parse", "preserve_order"] }
